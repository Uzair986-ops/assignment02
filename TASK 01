#include <iostream>
using namespace std;
void Function(int arr[], int size);
int main() {
	const int size = 5;
	int num[size] = { 1,2,3,4,5 };

	Function(num, size);
	return 0;
}
void Function(int arr[], int size) {
	for (int i = 0; i < size; i++)
	{
		cout << arr[i] << " ";
	}
}
